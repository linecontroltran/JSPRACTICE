let age = null; // undefined because no value here, so set it to null

console.log(age, age + 3, `the age is ${age}`);

// why use null? example is just to use as a placeholder if you have an input form with nothing in it i guess
